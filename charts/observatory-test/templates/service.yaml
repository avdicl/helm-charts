apiVersion: v1
kind: Service
metadata:
  name: {{ include "observatory-test.fullname" . }}
  labels:
    {{- include "observatory-test.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "observatory-test.selectorLabels" . | nindent 4 }}

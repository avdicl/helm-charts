apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "observatory-test.fullname" . }}-test-connection"
  labels:
    {{- include "observatory-test.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "observatory-test.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
